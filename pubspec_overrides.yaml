# melos_managed_dependency_overrides: config_repo,git_host_repo,ticket_host_repo,wack_cli
dependency_overrides:
  config_repo:
    path: repos/config_repo
  git_host_repo:
    path: repos/git_host_repo
  ticket_host_repo:
    path: repos/ticket_host_repo
  wack_cli:
    path: packages/wack_cli
